package model

// Enum types in this package are generated from the specs in ./enums by
// cmd/enum_codegen. Each spec sets its own output path (out), so regenerating
// is a single directory-wide pass.
//
//go:generate go run ../../cmd/enum_codegen -in ./enums
